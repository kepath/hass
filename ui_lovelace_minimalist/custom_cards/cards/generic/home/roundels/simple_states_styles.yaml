---
simple_states_styles:
  variables:
    device_entity_and_style_simple_states:
      use_themes: true
      normal_state:
        icon: "mdi:circle-outline"
        icon_color: "green"
        background_color: "green"
      warning_state:
        icon: "mdi:help-circle"
        icon_color: "yellow"
        background_color: "yellow"
      error_state:
        icon: "mdi:alert-circle"
        icon_color: "red"
        background_color: "red"
  state:
    - value: "normal"
      icon: >
        [[[
          var backupIcon =  "mdi:circle-outline";
          var returnedIcon;
          if (typeof variables?.device_entity_and_style_simple_states?.normal_state?.icon !== 'undefined') {
            if (variables?.device_entity_and_style_simple_states?.normal_state?.icon.includes('mdi:') ||
                variables?.device_entity_and_style_simple_states?.normal_state?.icon.includes('cbi:') ||
                variables?.device_entity_and_style_simple_states?.normal_state?.icon.includes('simple-icons:')) {
              returnedIcon = variables?.device_entity_and_style_simple_states?.normal_state?.icon;
            }
          }
          if (typeof returnedIcon !== 'undefined') {
            return returnedIcon;
          } else {
            return backupIcon;
          }
        ]]]
      styles:
        icon:
          - color: >
              [[[
                var returnedColor;
                var useThemesDefault = true;
                var useThemes;
                const colorOptions = ['grey', 'red', 'green', 'yellow', 'blue', 'purple', 'pink', 'amber', 'theme'];
                if (typeof variables?.device_entity_and_style_simple_states?.use_themes !== 'undefined') {
                  if (typeof variables?.device_entity_and_style_simple_states?.use_themes === 'boolean') {
                    useThemes = variables?.device_entity_and_style_simple_states?.use_themes;
                  } else {
                    useThemes = useThemesDefault;
                  }
                } else {
                  useThemes = useThemesDefault;
                }
                returnedColor = variables?.device_entity_and_style_simple_states?.normal_state?.icon_color;
                if (useThemes) {
                  for (let i = 0; i < colorOptions.length; i++) {
                    if (returnedColor.includes(colorOptions[i])){
                      returnedColor = "rgba(var(--color-" + returnedColor + "), 1)";
                    }
                  }
                }
                //hass.callService('system_log', 'write', {level:'warning',message:'returnedColor: ' + returnedColor});

                return returnedColor;
              ]]]
        img_cell:
          - background-color: >
              [[[
                var returnedColor;
                var useThemesDefault = true;
                var useThemes;
                const colorOptions = ['grey', 'red', 'green', 'yellow', 'blue', 'purple', 'pink', 'amber', 'theme'];
                if (typeof variables?.device_entity_and_style_simple_states?.use_themes !== 'undefined') {
                  if (typeof variables?.device_entity_and_style_simple_states?.use_themes === 'boolean') {
                    useThemes = variables?.device_entity_and_style_simple_states?.use_themes;
                  } else {
                    useThemes = useThemesDefault;
                  }
                } else {
                  useThemes = useThemesDefault;
                }
                returnedColor = variables?.device_entity_and_style_simple_states?.normal_state?.background_color;
                if (useThemes) {
                  for (let i = 0; i < colorOptions.length; i++) {
                    if (returnedColor.includes(colorOptions[i])){
                      returnedColor = "rgba(var(--color-" + returnedColor + "), 0.2)";
                    }
                  }
                }
                //hass.callService('system_log', 'write', {level:'warning',message:'returnedColor: ' + returnedColor});

                return returnedColor;
              ]]]
    - value: "warning"
      icon: >
        [[[
          var backupIcon =  "mdi:help-circle";
          var returnedIcon;
          if (typeof variables?.device_entity_and_style_simple_states?.warning_state?.icon !== 'undefined') {
            if (variables?.device_entity_and_style_simple_states?.warning_state?.icon.includes('mdi:') ||
                variables?.device_entity_and_style_simple_states?.warning_state?.icon.includes('cbi:') ||
                variables?.device_entity_and_style_simple_states?.warning_state?.icon.includes('simple-icons:')) {
              returnedIcon = variables?.device_entity_and_style_simple_states?.warning_state?.icon;
            }
          }
          if (typeof returnedIcon !== 'undefined') {
            return returnedIcon;
          } else {
            return backupIcon;
          }
        ]]]
      styles:
        icon:
          - color: >
              [[[
                var returnedColor;
                var useThemesDefault = true;
                var useThemes;
                const colorOptions = ['grey', 'red', 'green', 'yellow', 'blue', 'purple', 'pink', 'amber', 'theme'];
                if (typeof variables?.device_entity_and_style_simple_states?.use_themes !== 'undefined') {
                  if (typeof variables?.device_entity_and_style_simple_states?.use_themes === 'boolean') {
                    useThemes = variables?.device_entity_and_style_simple_states?.use_themes;
                  } else {
                    useThemes = useThemesDefault;
                  }
                } else {
                  useThemes = useThemesDefault;
                }
                returnedColor = variables?.device_entity_and_style_simple_states?.warning_state?.icon_color;
                if (useThemes) {
                  for (let i = 0; i < colorOptions.length; i++) {
                    if (returnedColor.includes(colorOptions[i])){
                      returnedColor = "rgba(var(--color-" + returnedColor + "), 1)";
                    }
                  }
                }

                return returnedColor;
              ]]]
        img_cell:
          - background-color: >
              [[[
                var returnedColor;
                var useThemesDefault = true;
                var useThemes;
                const colorOptions = ['grey', 'red', 'green', 'yellow', 'blue', 'purple', 'pink', 'amber', 'theme'];
                if (typeof variables?.device_entity_and_style_simple_states?.use_themes !== 'undefined') {
                  if (typeof variables?.device_entity_and_style_simple_states?.use_themes === 'boolean') {
                    useThemes = variables?.device_entity_and_style_simple_states?.use_themes;
                  } else {
                    useThemes = useThemesDefault;
                  }
                } else {
                  useThemes = useThemesDefault;
                }
                returnedColor = variables?.device_entity_and_style_simple_states?.warning_state?.background_color;
                if (useThemes) {
                  for (let i = 0; i < colorOptions.length; i++) {
                    if (returnedColor.includes(colorOptions[i])){
                      returnedColor = "rgba(var(--color-" + returnedColor + "), 0.2)";
                    }
                  }
                }

                return returnedColor;
              ]]]
    - value: "error"
      icon: >
        [[[
          var backupIcon =  "mdi:alert-circle";
          var returnedIcon;
          if (typeof variables?.device_entity_and_style_simple_states?.error_state?.icon !== 'undefined') {
            if (variables?.device_entity_and_style_simple_states?.error_state?.icon.includes('mdi:') ||
                variables?.device_entity_and_style_simple_states?.error_state?.icon.includes('cbi:') ||
                variables?.device_entity_and_style_simple_states?.error_state?.icon.includes('simple-icons:')) {
              returnedIcon = variables?.device_entity_and_style_simple_states?.error_state?.icon;
            }
          }
          if (typeof returnedIcon !== 'undefined') {
            return returnedIcon;
          } else {
            return backupIcon;
          }
        ]]]
      styles:
        icon:
          - color: >
              [[[
                var returnedColor;
                var useThemesDefault = true;
                var useThemes;
                const colorOptions = ['grey', 'red', 'green', 'yellow', 'blue', 'purple', 'pink', 'amber', 'theme'];
                if (typeof variables?.device_entity_and_style_simple_states?.use_themes !== 'undefined') {
                  if (typeof variables?.device_entity_and_style_simple_states?.use_themes === 'boolean') {
                    useThemes = variables?.device_entity_and_style_simple_states?.use_themes;
                  } else {
                    useThemes = useThemesDefault;
                  }
                } else {
                  useThemes = useThemesDefault;
                }
                returnedColor = variables?.device_entity_and_style_simple_states?.error_state?.icon_color;
                if (useThemes) {
                  for (let i = 0; i < colorOptions.length; i++) {
                    if (returnedColor.includes(colorOptions[i])){
                      returnedColor = "rgba(var(--color-" + returnedColor + "), 1)";
                    }
                  }
                }

                return returnedColor;
              ]]]
        img_cell:
          - background-color: >
              [[[
                var returnedColor;
                var useThemesDefault = true;
                var useThemes;
                const colorOptions = ['grey', 'red', 'green', 'yellow', 'blue', 'purple', 'pink', 'amber', 'theme'];
                if (typeof variables?.device_entity_and_style_simple_states?.use_themes !== 'undefined') {
                  if (typeof variables?.device_entity_and_style_simple_states?.use_themes === 'boolean') {
                    useThemes = variables?.device_entity_and_style_simple_states?.use_themes;
                  } else {
                    useThemes = useThemesDefault;
                  }
                } else {
                  useThemes = useThemesDefault;
                }
                returnedColor = variables?.device_entity_and_style_simple_states?.error_state?.background_color;
                if (useThemes) {
                  for (let i = 0; i < colorOptions.length; i++) {
                    if (returnedColor.includes(colorOptions[i])){
                      returnedColor = "rgba(var(--color-" + returnedColor + "), 0.2)";
                    }
                  }
                }

                return returnedColor;
              ]]]
